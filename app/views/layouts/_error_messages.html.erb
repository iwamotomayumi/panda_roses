<% if resource.errors.any? %>
  <div id="error_explanation">
    <h4 class="text-danger">必要事項を入力してください。</h4>
    <ul>
      <% resource.errors.full_messages.each do |message| %>
        <li class="text-danger"><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>