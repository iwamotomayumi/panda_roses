<% if resource.errors.any? %>
  <div id="error_explanation">
    <h2 class="text-danger">↓入力が不足しています😭</h2>
    <ul>
      <% resource.errors.full_messages.each do |message| %>
        <li class="text-danger"><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>