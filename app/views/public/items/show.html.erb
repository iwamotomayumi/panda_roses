<br>
<div class="container">
  <div class="row">
    <div class="col-md-5">
      <h4 style="color: #800000">🌹ItemDetails🌹︎</h4>
    </div>
  </div>
  <br>
  <div class="row">
    <div class="col-md-4">
        <%= image_tag @item.get_image, size: "350x300" %>
    </div>

    <div class="col-md-7 col-sm-10">
     <table class="table table-borderless">
      <tr>
        <td width="20%">商品名</td>
        <td width="60%"><%= @item.name %></td>
      </tr>
      <tr>
        <td>商品説明</td>
        <td width="600"><%= @item.introduction %></td>
      </tr>
      <tr>
        <td>税込価格<br><small>(税抜価格）</small></td>
        <td><strong>¥<%= @item.add_tax_price.to_s(:delimited) %></strong>
           (¥<%= @item.price.to_s(:delimited) %>)
        </td>
      </tr>
      <tr>
        <td><%= link_to items_path do %>
              <%= "<Back" %>
            <% end %>
        </td>
        <td></td>
      </tr>
    </table>
   </div>
  </div>
</div>