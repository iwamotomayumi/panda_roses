<br>
<div class="container">
  <div class="row">
    <div class="col-1"></div>
    <div class="col-11">
      <h4  style="color: #800000">🌹︎Item List🌹︎︎</h4>
      <br>
      <div class="item-wrap">
        <% @items.each do |item| %>
          <span class="item-box1 font-weight-bold">
            <%= link_to item_path(item.id) do %>
              <%= image_tag item.get_image, size: "160x160" %>
            <% end %>
            <div class="item-comment">
              <span class="item-body", style="font-size:15px"><%= item.created_at.strftime('%Y/%m/%d') %></span><br>
              <span class="item-body", style="font-size:15px"><%= item.name %></span><br>
              <span class="item-body", style="font-size:12px"><%= item.introduction %></span><br>
              <span class="item-body", style="font-size:12px">¥<%= item.add_tax_price.to_s(:delimited) %>（税込）</span>
            </div>
          </span>
        <% end %>
      </div>
    </div>
  </div>
  <div class= "row">
    <div class="col-5"></div>
      <div class= "col-2 text-center">
      <div class="pagination">
        <%= paginate @items %>
      </div>
    </div>
  </div>
</div>